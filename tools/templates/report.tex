%% The Janitor — Intelligence Report
%% Pandoc LaTeX template (pdflatex engine).
%%
%% Usage (called by `janitor report --format pdf`):
%%   pandoc input.md \
%%     --template tools/templates/report.tex \
%%     --pdf-engine=pdflatex \
%%     --toc --toc-depth=2 \
%%     -V title="..." -V date="..." -V version="..." \
%%     [-V logo=/abs/path/to/logo.png] \
%%     -o output.pdf

\documentclass[11pt,a4paper]{article}

% ── Typography ────────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{palatino}
\usepackage{courier}
\linespread{1.1}

% ── Page geometry ─────────────────────────────────────────────────────────────
\usepackage[margin=2.5cm, top=3.5cm, bottom=3cm, headheight=16pt]{geometry}

% ── Colour ────────────────────────────────────────────────────────────────────
\usepackage[table]{xcolor}
\definecolor{janitorNavy}{RGB}{15,40,80}
\definecolor{janitorRed}{RGB}{180,30,30}
\definecolor{tableStripe}{RGB}{245,245,250}
\definecolor{linkBlue}{RGB}{30,90,160}
\definecolor{shadecolor}{RGB}{245,245,250}

% ── Tables ────────────────────────────────────────────────────────────────────
\usepackage{longtable,booktabs,array,tabularx}
\usepackage{calc}   % required for \columnwidth arithmetic in longtable column specs
\usepackage{xfp}    % provides \real{} used by pandoc for proportional column widths
\renewcommand{\arraystretch}{1.3}
% Horizontal cell padding — prevents column content bleeding into adjacent cells
% during PDF conversion of wide tables.
\setlength{\tabcolsep}{6pt}
% Suppress booktabs rule-gap warnings from pandoc-generated tables.
\setlength{\aboverulesep}{0pt}
\setlength{\belowrulesep}{0pt}

% ── Graphics ──────────────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
% Prevent images from overflowing the page.
\setkeys{Gin}{width=\maxwidth,keepaspectratio}

% ── Header / footer ───────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
$if(logo)$
\fancyhead[L]{\includegraphics[height=0.7cm]{$logo$}}
$else$
\fancyhead[L]{\textbf{\textcolor{janitorNavy}{The Janitor}}}
$endif$
\fancyhead[R]{\small\textit{\textcolor{janitorNavy}{Intelligence Audit Report}}}
\fancyfoot[L]{\tiny\textit{\textcolor{gray}{Generated by The Janitor (thejanitor.app) --- see the breakdown in the attached CSV.}}}
\fancyfoot[C]{\small\textcolor{gray}{\thepage}}
\renewcommand{\headrulewidth}{0.4pt}

% ── Section styling ───────────────────────────────────────────────────────────
\usepackage{titlesec}
\titleformat{\section}
  {\large\bfseries\color{janitorNavy}}
  {}
  {0em}
  {}
  [\vspace{2pt}\color{janitorNavy}\titlerule\vspace{4pt}]
\titleformat{\subsection}
  {\normalsize\bfseries\color{janitorNavy}}
  {}
  {0em}
  {}
\titleformat{\subsubsection}
  {\small\bfseries\color{gray}}
  {}
  {0em}
  {}

% ── TOC styling ───────────────────────────────────────────────────────────────
\usepackage{tocloft}
\setlength{\cftsecindent}{0em}
\renewcommand{\cftsecfont}{\bfseries\color{janitorNavy}}
\renewcommand{\cftsecpagefont}{\bfseries\color{janitorNavy}}

% ── Hyperlinks ────────────────────────────────────────────────────────────────
\usepackage[colorlinks=true,
            linkcolor=linkBlue,
            urlcolor=linkBlue,
            citecolor=linkBlue,
            bookmarks=true,
            pdfencoding=auto]{hyperref}

% ── Code blocks (pandoc shaded / fancyvrb) ────────────────────────────────────
\usepackage{fancyvrb}
\usepackage{framed}
\newenvironment{Shaded}{\begin{snugshade}}{\end{snugshade}}
\newcommand{\AlertTok}[1]{\textcolor[rgb]{0.94,0.16,0.16}{#1}}
\newcommand{\AnnotationTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{\textit{#1}}}}
\newcommand{\AttributeTok}[1]{\textcolor[rgb]{0.77,0.63,0.00}{#1}}
\newcommand{\BaseNTok}[1]{\textcolor[rgb]{0.00,0.00,0.81}{#1}}
\newcommand{\BuiltInTok}[1]{#1}
\newcommand{\CharTok}[1]{\textcolor[rgb]{0.31,0.60,0.02}{#1}}
\newcommand{\CommentTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textit{#1}}}
\newcommand{\CommentVarTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{\textit{#1}}}}
\newcommand{\ConstantTok}[1]{\textcolor[rgb]{0.00,0.00,0.00}{#1}}
\newcommand{\ControlFlowTok}[1]{\textcolor[rgb]{0.13,0.29,0.53}{\textbf{#1}}}
\newcommand{\DataTypeTok}[1]{\textcolor[rgb]{0.13,0.29,0.53}{#1}}
\newcommand{\DecValTok}[1]{\textcolor[rgb]{0.00,0.00,0.81}{#1}}
\newcommand{\DocumentationTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{\textit{#1}}}}
\newcommand{\ErrorTok}[1]{\textcolor[rgb]{0.64,0.00,0.00}{\textbf{#1}}}
\newcommand{\ExtensionTok}[1]{#1}
\newcommand{\FloatTok}[1]{\textcolor[rgb]{0.00,0.00,0.81}{#1}}
\newcommand{\FunctionTok}[1]{\textcolor[rgb]{0.00,0.00,0.00}{#1}}
\newcommand{\ImportTok}[1]{#1}
\newcommand{\InformationTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{\textit{#1}}}}
\newcommand{\KeywordTok}[1]{\textcolor[rgb]{0.13,0.29,0.53}{\textbf{#1}}}
\newcommand{\NormalTok}[1]{#1}
\newcommand{\OperatorTok}[1]{\textcolor[rgb]{0.81,0.36,0.00}{\textbf{#1}}}
\newcommand{\OtherTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{#1}}
\newcommand{\PreprocessorTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textit{#1}}}
\newcommand{\RegionMarkerTok}[1]{#1}
\newcommand{\SpecialCharTok}[1]{\textcolor[rgb]{0.00,0.00,0.00}{#1}}
\newcommand{\SpecialStringTok}[1]{\textcolor[rgb]{0.31,0.60,0.02}{#1}}
\newcommand{\StringTok}[1]{\textcolor[rgb]{0.31,0.60,0.02}{#1}}
\newcommand{\VariableTok}[1]{\textcolor[rgb]{0.00,0.00,0.00}{#1}}
\newcommand{\VerbatimStringTok}[1]{\textcolor[rgb]{0.31,0.60,0.02}{#1}}
\newcommand{\WarningTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{\textit{#1}}}}

% ── Unicode safety net (pdflatex cannot handle math-range Unicode natively) ───
% Declare common symbols that may appear in antipattern descriptions or report
% text so pdflatex does not abort with "Unicode character not set up for use".
\usepackage{newunicodechar}
\newunicodechar{≥}{\ensuremath{\geq}}
\newunicodechar{≤}{\ensuremath{\leq}}
\newunicodechar{→}{\ensuremath{\rightarrow}}
\newunicodechar{←}{\ensuremath{\leftarrow}}
\newunicodechar{≠}{\ensuremath{\neq}}
\newunicodechar{…}{\ldots}

% ── Pandoc internals ──────────────────────────────────────────────────────────
% pandoc requires \tightlist to exist.
\providecommand{\tightlist}{%
  \setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}}

% ── Misc ──────────────────────────────────────────────────────────────────────
\usepackage{parskip}
\setlength{\parskip}{6pt plus 2pt minus 1pt}
\usepackage{microtype}

% ── pandoc highlighting macros (injected at compile time) ─────────────────────
$if(highlighting-macros)$
$highlighting-macros$
$endif$

% =============================================================================
\begin{document}
% =============================================================================

% ── Cover page ────────────────────────────────────────────────────────────────
\thispagestyle{empty}
\begin{center}
  \vspace*{2.5cm}
  $if(logo)$
  \includegraphics[width=5cm]{$logo$}\\[1.8cm]
  $else$
  \vspace{1.8cm}
  $endif$
  {\Huge\bfseries\color{janitorNavy} $if(title)$$title$$else$Intelligence Report$endif$}\\[0.6cm]
  {\large\color{gray} Code Integrity Audit}\\[1.2cm]
  \noindent{\color{janitorNavy}\rule{\textwidth}{0.5pt}}\\[1.0cm]
  {\normalsize\textbf{Generated:} $if(date)$$date$$else$\today$endif$}\\[0.4cm]
  $if(version)$
  {\normalsize\textbf{Engine:} \texttt{janitor v$version$}}\\[0.4cm]
  $endif$
  \vfill
  {\small\textcolor{gray}{For authorised recipients only.\\
  This document was generated automatically by The Janitor static analysis engine.}}
\end{center}
\newpage

% ── Table of contents ─────────────────────────────────────────────────────────
$if(toc)$
\tableofcontents
\newpage
$endif$

% ── Report body ───────────────────────────────────────────────────────────────
$body$

\end{document}
