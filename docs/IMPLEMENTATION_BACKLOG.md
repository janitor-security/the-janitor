# Implementation Backlog

Append-only dated ledger for local directive execution.

## 2026-04-14

- v10.1.9 directive executed: P1-1 Group 3 (ObjC + GLSL) taint producers landed; 23-grammar taint spine complete; native `janitor hunt` command forged (sourcemap ingestion, walkdir recursive scan, `Vec<StructuredFinding>` JSON output, 1 MiB circuit breaker, path traversal guard); 15 new tests; P2-7 jq-filter proposal logged.
- v10.1.6 directive executed: replaced static ESG energy constants with duration-based telemetry, extended taint producers into Swift/Scala, added SARIF/CEF strike artefacts, and rewrote the remaining-language priority stack toward Bash/Terraform/Nix.
