name: Dependency Review

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: write # required-by-design: action comments the dependency summary on the PR
    # Non-blocking by design: transient GitHub Dependency Graph API fetch failures
    # must not block Dependabot auto-merge. "Dependency Review" is intentionally
    # absent from branch-protection required_status_checks (Law W-VI). The Janitor
    # slop-gate (janitor-pr-gate.yml) is the sole blocking integrity check.
    continue-on-error: true
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            github.com:443

      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          fetch-depth: 0  # dependency-review-action fetches refs/pull/N/merge; shallow clone fails to resolve the merge-ref pack object

      - name: Dependency Review
        uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48 # v4
        with:
          fail-on-severity: moderate
          deny-licenses: GPL-2.0, GPL-3.0, AGPL-1.0, AGPL-3.0, LGPL-2.0, LGPL-2.1, LGPL-3.0
          comment-summary-in-pr: always
