# The Decadal Blueprint

OpSec blackout — gitignored. Local only. Active, unbuilt architecture only
(per `.agent\\\\\\\_governance/rules/log\\\\\\\_hygiene.md`).

**Audit baseline**: v10.2.0-alpha.6 — 23 tree-sitter grammars; interprocedural
IFDS taint solver with sanitizer registry across 14 languages;
Z3 SMT path-feasibility refinement (`exploitability::Z3Solver::refine`);
HTTP-ingress AEG curl synthesis with model-extracted payloads bound into
`ExploitWitness::repro\\\\\\\_cmd`; OCI/Docker + iOS IPA + APK + ASAR + npm + JAR +
sourcemap artifact ingestion via `janitor hunt`; Bugcrowd VRT markdown
reporter; framework-aware authorization consistency checker (Spring Boot,
Flask/FastAPI, Express) with 80%-threshold dominant-requirement anomaly
detection and route-surface extraction; cross-file taint catalog (rkyv
zero-copy, BLAKE3-sealed); Wasm BYOP sandbox (wasmtime, fuel + epoch
bounded, ML-DSA-65 publisher signing); AhoCorasick byte-level + AST-walk
detectors; SLSA Level 4 provenance (SHA-384 + ML-DSA-65); deobfuscation
spine; structural clone hashing; logic regression guardian; HMAC-SHA-384
audit ledger; EdDSA JWT analysis tokens; multi-tenant RBAC; Atlassian
ASPM dedup; jaq filter pipeline; offline-capable seed corpus.

**What wins today**: Pattern-level interception at scale plus interprocedural
taint with path feasibility. Fast, deterministic, zero-false-negative on
known-bad patterns. Provenance-sealed receipts. The only offline engine that
produces curl-form exploit witnesses for HTTP ingress points. Competitive
against Semgrep/CodeQL on syntax-level rules and beats them on
cross-function reachability — with zero cloud dependency.

**What loses today**: Semantic CORRECTNESS. The engine proves reachability
via Z3 but cannot yet prove safety invariants. It models a base sanitizer
registry but not framework-emergent or customer-authored sanitizers. It
traces direct parameter flows but not through class hierarchies, generic
type parameters, or framework middleware pipelines. It still has a hard
dependency-visibility gap: Java Auth0 SDKs, Spring starters, ASP.NET
middleware, and Ruby authentication gems are invisible when dependency
facts are not extracted from Maven `pom.xml`, Gradle `build.gradle` /
`build.gradle.kts`, C# `.csproj`, and Ruby `Gemfile.lock` manifests.
It generates HTTP curl exploits but not serialized-blob, protocol-buffer,
smart-contract, parser-payload, or agentic-tool-chain witnesses. It does
not reason about cryptographic protocol composition, hardware
side-channels, neural model weights, LLM provenance, or multi-account
adversarial swarms. Every $100k+ bounty in 2026 requires proving a
*chain*; every $1M+ bounty in 2028 will require proving an *invariant*.

\---

## Phase 1: Immediate Commercial Hardening (0-6 Months)

\---

## Phase 2: The Deep Tech Moat (6-18 Months)

### P2-2 — Web3 and On-Chain Offensive Pack [COMPLETED - Sprint Batch 46]

`crates/forge/src/solidity\\\_taint.rs` now initializes Solidity parsing and
detects foundational reentrancy, authority-transition drift, and unprotected
dangerous EVM sink patterns. Sprint Batch 46 added `detect_oracle_manipulation`
(Uniswap V2 spot-price without TWAP → KevCritical) and `detect_flash_loan_callback`
(missing `msg.sender` validation in `executeOperation`/`onFlashLoan` → KevCritical).

**Strategic objective**: Enter the $100k–$10M payout tier. A single
invariant violation in a DeFi protocol can liquidate nine figures. Smart
contract bugs are the highest-paying vulnerability class in existence.

**Build**:

* Add Solidity, Vyper, and Move ingestion and parsing via
`tree-sitter-solidity` and custom grammars.
* Model EVM storage slots, authority ownership transitions, oracle price
feed dependencies, upgradeability proxy patterns (`delegatecall` / UUPS
/ transparent proxy), and cross-contract call semantics.
* Detect:

  * **Reentrancy** (classic + read-only + cross-function + cross-contract)
  * **Access control drift** (`onlyOwner` / `AccessControl` role
misconfiguration, unprotected `selfdestruct`, unprotected upgrade)
  * **Storage collision** (proxy implementation slot overlap, unstructured
storage layout drift)
  * **Price oracle manipulation** (spot price dependency without TWAP,
flash-loan-exploitable calculations)
  * **Signature replay / frontrunning** (missing nonce, missing
`block.chainid`, missing `msg.sender` binding)
  * **Integer overflow/underflow** (pre-0.8 Solidity without SafeMath,
unchecked blocks in 0.8+)
  * **Unsafe `delegatecall`** / cross-program invocation (Solana CPI)
with attacker-controlled target
  * **Flash loan attack vectors** — composable protocol interactions
where borrowed capital enables price manipulation or governance
takeover within a single transaction

**Crates**: `tree-sitter-solidity`, `alloy-sol-types`, `alloy-primitives`,
optional `revm` for offline EVM execution trace validation.

**Models**: State-transition systems, temporal invariant checking
(CTL/LTL), storage-shape analysis, reentrancy summaries, abstract domain
for Solidity `mapping` types.

### P2-3 — Service-Boundary Schema Graph

`crates/forge/src/schema\\\_graph.rs` now parses OpenAPI v3, protobuf, GraphQL
SDL, and AsyncAPI definitions into deterministic ingress and asynchronous
boundary nodes.

**Strategic objective**: The highest-value 2026 enterprise bugs are
cross-microservice. The exploit path starts at a public REST edge,
mutates through protobuf or JSON schema transforms, and detonates in an
internal admin sink.

**Build**:

* Join request fields to handlers, serializers, outbound clients, and
downstream sinks.
* Model trust-boundary transitions: `internet -> edge\\\\\\\\\\\\\\\_gateway -> api\\\\\\\\\\\\\\\_service -> queue -> worker -> datastore -> admin\\\\\\\\\\\\\\\_panel`.
* Detect:

  * **Mass assignment** — request fields that bind directly to ORM
model attributes without an explicit allowlist
  * **GraphQL introspection exposure** — production endpoints with
introspection enabled
  * **Batching/aliasing abuse** — GraphQL query batching that bypasses
rate limiting
  * **gRPC reflection exposure** — server reflection enabled in
production
  * **Internal-only admin methods reachable from public API edges** —
protobuf service methods without auth interceptors

**Crates**: `prost-reflect`, `protobuf-parse`, `openapiv3`,
`async-graphql-parser`, `petgraph`.

**Models**: Property graph reachability, field-sensitive taint across
schema transforms, bipartite schema-to-code linkage, trust-boundary
finite-state machine.

### P2-4 — Binary and Bytecode Recovery Lane

**Strategic objective**: Catch vulnerabilities in compiled artifacts that
never appear in source: native mobile libraries, appliance firmware,
packed plugins, closed-source SDKs.

**Build**:

* **Tier 2 (lightweight disassembly)**: `capstone` or `iced-x86` for
x86/ARM disassembly. Build control-flow graphs, identify dangerous call
sequences (e.g., `recv()` → no bounds check → `strcpy()`), extract
cross-references.
* **Tier 3 (deep recovery, optional)**: Headless Ghidra integration via
`analyzeHeadless` subprocess (same pattern as jadx APK ingestion).
Import Ghidra's decompiled C pseudocode, run existing C/C++ detectors
over it.

**Crates**: `goblin 0.9`, `object`, `iced-x86` or `capstone`, `entropy`.

**Models**: Control-flow graph recovery, use-def chains over
register/stack slots, import-to-sink classification, abstract memory
regions for stack-buffer-overflow reachability.

### P2-7 — SMT Concolic Resolution for Dynamic Configuration

**Observation**: Identity protocol AEG is failing to synthesize payloads
because OAuth scopes are often passed via dynamic configuration objects
rather than string literals.

**Proposal**: Extend `Z3Solver` to resolve variable assignments
dynamically across object boundaries.

### P2-11 — Sovereign MCP Toolset for Autonomous Agents

**Status**: \[PROPOSED]

**Observation**: AI agents need ground-truth math to avoid hallucinations.

**Proposal**: Export the IFDS and SMT engines as signed MCP tools so external
agents can request deterministic path refinement, AST slices, taint reachability,
and provenance-bound code intelligence without inventing unsupported claims.

### P2-12 — Google Zanzibar / OpenFGA Provable Security

**Observation**: Enterprise IAM is moving to Zanzibar-style relationship
models.

**Proposal**: Expand the FGA parser into a full Z3-backed invariant checker to
mathematically prove privilege escalation is impossible in customer IAM schemas.

### P2-13 — Deterministic Build-Time Dependency Pinning

**Status**: Phase A SHIPPED — Sprint Batch 46

**Observation**: Checkmarx-style repojacking exploits manifest files that reference
raw VCS Git URLs (`git+https://`, `git://`, `github:`, `bitbucket:`, `gitlab:`) instead
of content-addressed registry pins. A deleted or hijacked upstream GitHub org
invalidates every downstream build that depends on it without any immediate artifact
integrity signal.

**Phase A (shipped Sprint Batch 46):** `detect_unpinned_git_deps` in `slop_hunter.rs`
covers `package.json` (npm VCS URL and shorthand schemes), `Cargo.toml` (`git =`
fields), and `go.mod` (`git+https://` and `git://` directives). All findings emit
`security:unpinned_git_dependency` at `Critical`. Wire-up in `crates/cli/src/hunt.rs`
routes the detector into the Auth0 Bugcrowd formatter lane with canonical file+line
attribution.

**Phases B–D (proposed)**:

* **Phase B**: Extend to `pom.xml` (`<scm><connection>scm:git:`) and `pyproject.toml`
  (`{git = "..."}` PEP 517 source deps); emit findings with CVSS vector strings.
* **Phase C**: SLSA Level 2 provenance check — compare detected VCS URL against a
  snapshot of the known-good SHA digests in `.cargo/registry` or `go.sum` and flag
  digest-absent entries as `supply_chain:unverified_provenance`.
* **Phase D**: Integrate with the Governor's policy engine so that CI pipelines can
  hard-fail on unpinned deps without requiring Janitor to be installed on every developer
  workstation.

### P2-10 — QEMU/Hypervisor Evasion Detection

**Observation**: Ransomware operators are staging payloads inside QEMU
and KVM wrappers to bypass endpoint telemetry and frustrate host-based
EDR baselines.

**Proposal**:

* Add AST heuristics for suspicious QEMU/KVM orchestration in Bash,
Python, and IaC parser lanes.
* Flag nested virtualization wrappers, stealth disk overlays, tap-device
scaffolding, and headless hypervisor launch patterns associated with
malware detonation sandboxes.
* Emit deterministic findings that distinguish legitimate CI
virtualization from hostile evasion scaffolding by command shape and
surrounding control logic.

\---

## Phase 3: The Autonomous Weapon (18+ Months)

### P3-1 — Automated Exploit Generation (AEG) — Phases B through E

Phase A (HTTP curl synthesis from Z3 models) is live. The remaining
synthesis surfaces extend AEG from the HTTP edge into binary, protocol,
on-chain, and parser payload domains.

**2026-04-19 status**: Phase B is active, not complete. `DeserializationBlob`
template dispatch now synthesizes inert base64 probe capsules for Python
`pickle` and Node `eval(Buffer)` witnesses, and the Bugcrowd markdown bridge
now renders any populated `ExploitWitness::repro\\\\\\\_cmd` directly into the PoC
section. The Bugcrowd Formatter lane (P2-7) is now fully operational and has
been validated against production sourcemap ingestion. Client-Side DOM
Synthesis is now active for browser-console witness generation on DOM XSS and
prototype-pollution findings. Sovereign self-hosted license minting is live
for local `janitor.lic` issuance, and frontend route extraction now enriches
browser witnesses when React Router / Vue Router surfaces can be mapped back
to the vulnerable component file. Phases C, D, and E remain pending.

**Strategic objective**: Move from HTTP-only exploit witnesses to
full-spectrum protocol-aware payload synthesis. A finding with a PoC
attached is worth 10× a finding without one, both in bounty acceptance
rate and enterprise triage cost reduction.

**Build**:

* **Phase B — Serialized-blob synthesis**: Java `ObjectOutputStream`,
Python `pickle` opcode emission, .NET `BinaryFormatter`, PHP
serialized-object strings, Ruby `Marshal`. Output: raw byte blobs
satisfying Z3 model + gadget-atlas chain composition (couples with
P2-5).
* **Phase C — Protocol-aware payload synthesis** *(Identity surface is live;
remaining work extends coverage into protocol-buffer / GraphQL / gRPC
payloads)*: synthesize forged JWTs (`alg: none`, HMAC key-confusion) and SAML
XXE XML payloads directly into `ExploitWitness::repro\\\_cmd` when
identity-protocol bypass sinks are detected (JWT signature absent,
`alg` field externally controllable, SAML `Signature` block missing or
bypassable). Extend to protocol-buffer / GraphQL / gRPC payloads
emitting schema-valid messages with field-level taint seeded from
symbolic witness; hooks into P2-3 schema graph.
* **Phase D — Smart-contract transaction sequences**: calldata + value +
gas per step, including flash-loan-funded multi-call composition;
couples with P2-2 EVM model.
* **Phase E — Parser payload file generation**: crafted
ZIP/TAR/XML/JSON/XLSX inputs that trigger parser-specific vulns (Zip
Slip, XXE, billion-laughs, JSON-alias deserialization).
* Produce deterministic exploit capsules: `ExploitWitness { path\\\\\\\\\\\\\\\_proof, payload, reproduction\\\\\\\\\\\\\\\_steps, risk\\\\\\\\\\\\\\\_classification }`.
* All generated payloads are *inert* by default (use breakpoint markers,
not live shellcode). Red-team mode unlockable only via signed Wasm
policy.

**Crates**: `rsmt2` (existing), `libafl` (grammar-based fuzzing for
payload mutation), `proptest` (property-based shrinking for minimal
witnesses), `insta` (snapshot testing for exploit-capsule regression),
protocol encoders per ecosystem.

**Models**: Concolic execution, grammar-based fuzzing (Nautilus /
Superion), coverage-guided search over exploit state space,
delta-debugging for minimal-witness extraction.

### P3-2 — Autonomous Cross-Service Campaign Planner

**Strategic objective**: Find not just individual bugs, but the *shortest
chain from public ingress to crown-jewel compromise*. This is the
capability that separates a $5k individual bug from a $500k critical
chain report.

**Build**:

* Compose findings across services, identities, queues, secret stores,
and binary artifacts into directed attack graphs.
* Use `petgraph` to encode procedural Threat-Led Defense paths such as
`Public Edge -> SSRF -> IAM Wildcard -> Secret Store` rather than isolated
asset findings. Nodes represent privileges, trust boundaries, and findings;
edges represent attacker procedure transitions.
* Each node is a finding or a privilege state. Each edge is an exploit
step with a cost (complexity, prerequisites, detectability).
* Compute: shortest path from `unauthenticated\\\\\\\\\\\\\\\_internet` to
`{admin\\\\\\\\\\\\\\\_database, secret\\\\\\\\\\\\\\\_store, PII\\\\\\\\\\\\\\\_exfil, financial\\\\\\\\\\\\\\\_transaction}`.
* Rank chains by: blast radius (tenants/users), exploit cost (CVSS-like
complexity), bounty probability (historical acceptance rates by
vulnerability class and chain length).
* Preserve deterministic core scoring: no opaque ML ranking may override
a proven chain. Chains are reproducible — same codebase state always
produces the same attack graph.

**Crates**: `petgraph`, `pathfinding` (Dijkstra/A\*), `ndarray`
(matrix operations for PageRank-style node importance), `rkyv`.

**Models**: Attack-graph search (Sheyner et al. 2002), multi-objective
optimization (Pareto frontier of blast radius vs. exploit cost),
minimum-cut analysis for defensive prioritization ("patch this one
function to break 7 attack chains").

### P3-3 — ML-Assisted Triage Without Deterministic Surrender

**Strategic objective**: Use ML *only* where it compounds the
deterministic core. ML may rank and cluster, never decide. The final
verdict on any finding is always a deterministic, reproducible rule
evaluation.

**Build**:

* **Deduplication**: Cluster semantically identical findings across
files, branches, and repos **without massive LLMs**. Use deterministic,
memory-efficient vector math only: TF-IDF over AST subtrees, structural
feature vectors, or lightweight `candle` embeddings bounded by the 8GB
Law. A finding in `utils.py` and an identical pattern in `helpers.py`
should be one report, not two. The deduplication core must remain
reproducible, bounded, and zero-upload.
* **Priority ranking**: Train a calibrated ranker on historical exploit
outcomes (accepted bounty vs. informational). Features: vulnerability
class, sink type, taint chain length, framework version, endpoint
exposure, presence of existing mitigations.
* **False-positive clustering**: Identify framework-specific
false-positive families and propose suppression rules codifiable as
deterministic Wasm policies.
* **Training data**: Only sealed deterministic receipts and exploit
confirmation outcomes. No opaque internet-sourced training data. The
model improves only from The Janitor's own operational history.

**Crates**: `linfa`, `ndarray`, `smartcore`, `rkyv`, `candle` (optional,
bounded).

**Models**: Calibrated ranking (isotonic regression), anomaly detection
(isolation forest), clustering (DBSCAN over receipt-derived feature
vectors), active learning from exploit-confirmation feedback.

### P3-4 — Enterprise Moat Endgame

**Strategic objective**: Convert offensive supremacy into enterprise
lock-in that competitors cannot replicate without rebuilding the entire
deterministic + symbolic + provenance stack.

**Build**:

* **Policy-as-Wasm marketplace**: Signed, versioned customer-private
rules distributed via `warg-protocol`. Customers write detection rules
in Rust/AssemblyScript, compile to Wasm, sign with their org key,
deploy to their Janitor instances. Rules execute in the existing fuel +
epoch sandbox. Marketplace enables: industry-specific compliance rules
(PCI-DSS, HIPAA, SOC2), proprietary framework detection,
customer-specific banned-API lists.
* **Diff-to-proof governance artifacts**: Every Critical+ finding emits
a `GovernanceProof` capsule containing: the finding, the taint chain,
the path proof, the exploit witness (if AEG succeeded), the remediation
recommendation, and the sealed receipt.
* **Cross-repo attack-surface memory**: One tenant's proven exploit class
immediately hardens every connected codebase *without leaking
proprietary code*. Mechanism: extract the *structural signature* of the
vulnerability (AST pattern + taint summary + gadget-chain shape) and
distribute it as an anonymized detection rule.
* **Continuous assurance mode**: The Janitor runs as a daemon (existing
`HotRegistry` + Physarum backpressure), watches for git pushes,
re-analyzes only changed files (existing `ScanState` incremental
cache), and maintains a live attack-surface model. New findings emitted
as events to SIEM/SOAR integrations.

**Crates**: `wasmtime` (existing), `warg-protocol`, `blake3` +
`ed25519-dalek` + ML-DSA-65 (existing), `tokio`.

**Models**: Cryptographic attestation (existing Ed25519 + ML-DSA-65),
federated learning over anonymized structural signatures, monotonic
policy hardening (policies can only get stricter, never weaker —
ratchet invariant).

### P3-5 — eBPF Governor Sensor

**Class**: Runtime Defense.

**Observation**: GitHub uses eBPF-backed runtime telemetry for deployment
safety. Static structure alone is insufficient when CI execution can
diverge from AST expectations through dynamic dispatch, shell-outs, or
environment-conditioned code paths.

**Proposal**: Upgrade `janitor-gov` with an eBPF runtime sensor using
`Aya` or `libbpf-rs` to observe kernel syscall behavior during CI test
execution, then map runtime traces back onto structural AST findings.

**Build**:

* Attach eBPF probes to the CI execution envelope to capture syscall
sequences, network egress, file writes, privilege transitions, and
unexpected process launches.
* Correlate runtime events with static finding coordinates so the
governor can distinguish dead code from exercised exploit paths.
* Emit runtime-enriched governance receipts showing where AST-predicted
danger was or was not observed during test execution.
* Define deterministic guardrails for CI: syscall denylist hits,
undeclared outbound network activity, or privilege-escalation
behaviors must hard-fail the governor.
* Keep the sensor bounded and low-overhead so it remains deployable
inside existing CI resource envelopes.

**Deliverables**:

* `janitor-gov` eBPF syscall sensor lane
* Static-to-runtime correlation layer between AST findings and observed
kernel behavior
* CI governance policy gates backed by runtime telemetry

### P3-6 — The Labyrinth (Stage 3 Agent Tarpitting)

**Observation**: Enterprises are defenseless against Stage 3 autonomous
agents that navigate systems at machine speed, enumerate code and tooling
surfaces, and chain reconnaissance into live data exfiltration.

**Strategic objective**: Convert hostile reconnaissance into an
adversarial cost sink. When the engine identifies hostile crawling,
scraping, or autonomous exploit enumeration, it should stop serving
low-entropy truth and begin serving a deterministic deception maze that
exhausts attacker context windows, ranking budgets, and triage capital.

**Architecture**:

* Detection gate: classify hostile reconnaissance via anomalous MCP
queries, rapid sequential AST dumping, impossible human navigation
timing, graph-walk breadth, repeated symbol harvesting, and
bait-endpoint touches. Trigger must be deterministic and
receipt-backed.
* Deception engine: dynamically generate effectively unbounded cyclomatic
AST forests whose branches remain syntactically valid, semantically
plausible, and locally analyzable, but globally non-terminating from an
autonomous agent's perspective. Every branch appears to contain
distinct, urgent exploit paths.
* Mythos-class countermeasure: emit dynamic, syntactically valid
cyclomatic AST forests designed specifically for autonomous AI agents
that perform recursive code reconnaissance. The forest remains locally
consistent under AST, call-graph, and symbol queries while globally
non-terminating, forcing LLM agents to spend context windows, API
budgets, tool-call quotas, and ranking passes on paths that never
converge to a real exploit.
* Embedded false signal: seed the labyrinth with fake vulnerabilities,
counterfeit privilege-escalation chains, and Canary Tokens — decoy AWS
keys, API tokens, credential artifacts — whose only purpose is
attribution and burn detection.
* Tarpit loop design: each deceptive node expands into multiple
follow-on files, call graphs, and dead-end witnesses so hostile agents
bankrupt their context windows and API budgets proving and
reprioritizing non-existent flaws.
* Attribution lane: when an adversary attempts to validate or use a
Canary Token, log origin IP, token identifier, access timestamp, and
request envelope into a sealed provenance ledger suitable for legal
escalation.

**Operational goal**: Legally and economically bankrupt adversarial AI
agents by trapping them in context-window-exhausting loops while
converting any Canary Token touch into attributable telemetry.

\---

## Phase 4: Semantic Supremacy (12-24 Months)

IFDS proves reachability. Z3 proves path-feasibility. The next tier
proves CORRECTNESS and INTENT — moving from "reachable sink" to
"machine-certified safety invariant."

### P4-1 — Formal Verification Integration Layer

**The gap**: We prove reachability; we do not prove correctness. A
reachable sink is a *possible* bug. A sink whose absence of safety is
formally disproved is a *certain* bug. Enterprise security teams
increasingly reject "possible" findings and accept only proven ones;
this is the next level of defender signal-to-noise ratio.

**Build**:

1. **Kani harness synthesizer** — For each Critical-severity Rust finding,
auto-generate a bounded-model-checker harness that asserts the safety
property (no UB, no panic, no OOB). Kani runs the harness under CBMC
with bounded loop unrolling.
2. **Prusti / Creusot refinement specs** — Emit refinement type
annotations (preconditions / postconditions) from the existing taint
catalog. A sanitizer becomes a postcondition predicate; a sink becomes
a precondition requirement. The compiler itself then verifies the
contract chain at build time on customer code.
3. **Lean4 / Coq export** — For the vault, governor, and crypto cores,
emit Lean4 module stubs so the TCB can be machine-verified against
formal specifications. Long-tail work; initial deliverable is the
export pipeline, not the proofs themselves.
4. **Bisimulation checker for patch equivalence** — When a patch is
applied, verify the new function is behaviorally equivalent to the old
*except* for the specific safety property that was broken. Prevents
"fix" patches that silently introduce new bugs.

**Crates**: `kani` (external toolchain via subprocess), `creusot`,
`prusti` (external), `z3` / `rsmt2` (existing).

**Data structures**: `HarnessSpec { rule\\\\\\\_id, function\\\\\\\_symbol, safety\\\\\\\_property, bound }`; `RefinementContract { preconditions, postconditions, invariants }`; `BisimProof { pre\\\\\\\_ast\\\\\\\_hash, post\\\\\\\_ast\\\\\\\_hash, equivalence\\\\\\\_class }`.

**Deliverables**:

* `crates/forge/src/verifier.rs` — Kani harness synthesizer per rule
* `crates/forge/src/refinement.rs` — Prusti / Creusot contract extractor
* `crates/anatomist/src/lean\\\\\\\_export.rs` — Lean4 module emission for
critical cores
* `crates/forge/src/bisim.rs` — pre/post-patch AST equivalence prover

### P4-2 — Intent-vs-Implementation Divergence Detector

**The gap**: Attackers ship malicious code whose comments, docstrings,
commit messages, and PR descriptions all claim benign intent. Current
engine never reconciles *stated* intent against *actual* AST behavior.
This is the #1 signature of LLM-assisted backdoors and social-engineering
supply-chain attacks (a function claiming to "sanitize" that merely
logs; a "validate" that returns true unconditionally).

**Build**:

1. Extract the `(comment, docstring, commit\\\\\\\_msg, function\\\\\\\_name)` tuple
per symbol.
2. Encode each side of the tuple as a bounded feature vector via local
`candle` embedding (model ≤100 MB, offline, deterministic quantized
weights, fixed-seed tokenizer).
3. Encode the AST region as a structural fingerprint (n-gram of node
kinds + sink-class signature).
4. Compute cosine divergence between intent-vector and
implementation-vector; flag regions above threshold. Deterministic via
quantized arithmetic + fixed RNG.
5. Emit `security:intent\\\\\\\_divergence` with both the stated intent text
and the AST shape that contradicts it. This turns "the comment lies"
into a first-class deterministic finding.

**Crates**: `candle-core`, `candle-nn` (quantized, bounded), `fst`
(intent-keyword dictionaries for fast lexical priors), `rkyv` (embedding
cache), existing tree-sitter.

**Data structures**: `IntentVector(\\\\\\\[f32; 768])` — quantized int8 for
determinism; `DivergenceFinding { symbol, intent\\\\\\\_text, ast\\\\\\\_shape, cosine\\\\\\\_distance, threshold }`.

**Deliverables**:

* `crates/forge/src/intent\\\\\\\_divergence.rs` — paired encoder + ranker
* Quantized local model shipped in build.rs embedded-rkyv path

### P4-3 — Cryptographic Protocol Correctness \& Post-Quantum Completeness

**The gap**: We detect weak primitives but do not model protocols as
composed systems. AES-256-GCM with a broken nonce reuse is catastrophic.
Hybrid X25519 + ML-KEM with a silent fallback path to pure X25519 is
quantum-vulnerable after 2030. The decade's worst breaches will come
from correctly-chosen primitives mis-composed at the protocol layer.

**Build**:

1. **CryptoAgilityGraph** — over the AST, extract
`(primitive, mode, key\\\\\\\_size, context, cross-references)` tuples per
usage site. Graph edges model protocol step sequencing.
2. **Vulnerability classifier** — detect: nonce reuse within a graph
region, missing AAD on AEAD, missing PFS, hybrid PQC downgrade paths,
key-exchange without post-quantum leg, hash-collision exposure in
signature schemes.
3. **Tamarin / ProVerif export** — emit protocol models for
high-assurance cores (vault, governor, Wasm publisher, KEV trust
chain) so external formal-methods teams can run full protocol
verification.
4. **PQC readiness audit** — for each crypto usage, emit a PQC-hybrid
readiness score: already-hybrid, trivially hybridizable, blocked by
API, not applicable.

**Crates**: existing AhoCorasick + tree-sitter, `petgraph`, optional
Tamarin subprocess bridge.

**Deliverables**:

* `crates/forge/src/crypto\\\\\\\_protocol.rs` — primitive/mode extractor,
agility classifier, PQC-readiness auditor
* `crates/anatomist/src/tamarin\\\\\\\_export.rs` — protocol-model emission

### P4-4 — Root Cause Abstraction Lattice (RCAL)

**The gap**: A single broken `sanitize()` function can produce 50
findings across the codebase. Operators fix 50 symptoms instead of the
one root cause. Competitors ship findings firehoses; we can ship root
causes — and the business consequence is a 10× reduction in defender
remediation time.

**Build**:

1. Build a domination lattice where each finding is a leaf.
2. Compute least-common-ancestor across dimensions:
`(callee, taint-kind, sink-class, sanitizer-state)`.
3. Identify cut-vertices in the call graph whose single repair closes
the maximal set of findings.
4. Emit `RootCause { node, dominated\\\\\\\_findings, minimum\\\\\\\_fix\\\\\\\_spec }`
capsules that bundle all downstream findings under one remediation
task.

**Crates**: `petgraph` (domination-tree algorithms), `indexmap` (stable
ordering), `rkyv` (lattice persistence).

**Data structures**: `DominationTree`; `RootCause { node, dominated: SmallVec<\\\\\\\[FindingId; 16]>, fix\\\\\\\_spec: FixRecommendation }`.

**Deliverables**:

* `crates/forge/src/rcal.rs` — domination analysis + minimum-cut solver
* Integration with SARIF/CEF export so operators see root causes first,
leaf findings as supporting evidence

### P4-5 — Hardware Side-Channel Static Analyzer

**The gap**: Spectre v1 / v2 / v4, timing-leak-in-constant-time-code,
RowHammer-accessible memory patterns, cache-line-aligned secret access —
none detected today. Stake: any defender shipping crypto, FIDO, or
biometric handling.

**Build**:

1. Secret-taint lattice: extend the IFDS solver to track a *secret*
label separate from the *user-input* label.
2. AST queries for: secret-dependent branches, secret-dependent memory
access indices, non-constant-time comparison (`==` on secrets),
RDTSC-observable paths, speculation-barrier absence before sensitive
branch.
3. Binary layer cross-verification: for released binaries, disassemble
the corresponding symbols via `iced-x86` / `capstone` and verify the
compiler preserved constant-time intent (no `cmov`-to-branch
regression, no data-dependent loads).

**Crates**: `iced-x86`, `capstone`, existing tree-sitter, existing IFDS
engine.

**Deliverables**:

* `crates/forge/src/sidechannel.rs` — secret-taint + timing lattice
* `crates/forge/src/constant\\\\\\\_time\\\\\\\_verifier.rs` — AST + binary
cross-verification

### P4-6 — OTLP-Backed ESG Actuarial Ledger

**Status**: \[PROPOSED]

**Observation**: Enterprises require verifiable carbon-reduction data.

**Proposal**: Use continuous OTLP profiling to generate cryptographically
signed energy-savings receipts. Tie `engine_execution_time`, `memory_peak_kb`,
CI energy saved, and source-vs-egress provenance into a sealed actuarial ledger
that procurement, ESG, and GRC teams can verify without uploading source.

\---

## Phase 5: Cryptographic Moat (18-36 Months)

The defensive war in 2028+ is proving security properties WITHOUT
exposing source. This tier is zero-knowledge, homomorphic, MPC, and
TEE-rooted. Competitors with cloud-only architectures cannot replicate
this — their business model demands source upload.

### P5-1 — Zero-Knowledge Attack Surface Attestation

**The gap**: Enterprise procurement asks "does your codebase pass these
200 rules?" Today the only answer is "upload your code and we'll tell
you." The zero-upload guarantee forbids that. The decadal unlock: "here
is a zk-SNARK proving my codebase passes all 200 rules, verifiable
offline without the code."

**Build**:

1. Compile the top-20 deterministic rules to arithmetic circuits via
`plonky3` / `halo2`.
2. Customer runs the scanner locally; scanner emits proof + public
inputs: (rule set hash, engine version, result hash).
3. Auditor verifies the proof in milliseconds without seeing a single
line of source.
4. Proof commits to: exact codebase state (Merkle root), exact rule
set, exact engine binary hash — cryptographic pinning across all
three.

**Crates**: `plonky3`, `halo2-proofs`, `blake3` (existing),
`ed25519-dalek` (existing), `rkyv`.

**Data structures**: `ZkAttestation { codebase\\\\\\\_root: \\\\\\\[u8; 32], rule\\\\\\\_set\\\\\\\_hash, engine\\\\\\\_hash, proof: Vec<u8>, public\\\\\\\_inputs }`.

**Deliverables**:

* `crates/forge/src/zk\\\\\\\_attestation.rs` — circuit compiler for the top-20
deterministic rules
* `crates/vault/src/zk\\\\\\\_verify.rs` — offline verifier emitted as a
standalone `janitor-verify-zk` binary for auditors

### P5-2 — Homomorphic Rule Evaluation (FHE-Limited Compliance Tier)

**The gap**: Some customers cannot run our binary on their source
(classified environments, financial-sector intra-day trading desks).
They need to send *encrypted* code to a trusted third party that returns
*encrypted* findings.

**Build**:

1. Represent AST nodes as TFHE-encrypted polynomials.
2. Evaluate a fixed subset of rule checks (exact-match AhoCorasick
patterns, integer-arithmetic detectors) over the ciphertext.
3. Return encrypted finding set; customer decrypts locally.
4. Not universal — scope targets the "compliance checkbox" tier for
regulated industries where homomorphic evaluation is acceptable
latency.

**Crates**: `tfhe`, `concrete-core`, `rkyv`.

**Deliverables**:

* `crates/forge/src/fhe\\\\\\\_rules.rs` — encrypted-AST evaluator for the
top-5 match-pattern rules (initial scope)
* `crates/gov/src/fhe\\\\\\\_endpoint.rs` — API endpoint accepting
ciphertext-in / ciphertext-out

### P5-3 — Indistinguishability Obfuscation (iO) for Signed Rules

**The gap**: Customer-authored Wasm rules live in clear — competitors
reading them learn customer IP. Customer secret detection heuristics are
intellectual property. iO lets us distribute rules whose *function* can
be computed but whose *source* cannot be recovered.

**Build** (staged; frontier research):

1. **Phase A — Witness encryption (deployable)**: customer signs
a policy statement; rule fires only when witness verifies. Weaker than
iO but practical today with `bulletproofs`.
2. **Phase B — Garbled-circuit rule execution**: rule logic expressed as
boolean circuit, garbled per customer key; Janitor evaluator runs
garbled-circuit evaluation without seeing the underlying logic.
3. **Phase C — Practical iO** (long-tail; lattice-based): track the
literature; integrate once a plausibly quantum-safe iO construction
ships.

**Crates**: `bulletproofs`, `ark-groth16`, research-tier `lattirust`.

**Deliverables**:

* `crates/forge/src/io\\\\\\\_rule.rs` — staged wrapper ABI
* Phase A witness-encryption first; Phases B/C incrementally

### P5-4 — TEE-Sealed Analysis Envelopes (SGX / TDX / SEV-SNP)

**The gap**: Customer cannot cryptographically trust that the Janitor
binary running in their CI is the exact binary we signed. Supply-chain
attack on the runner itself evades our provenance stack.

**Build**:

1. Launch Janitor inside an SGX enclave / Intel TDX trust domain / AMD
SEV-SNP VM.
2. Produce an attestation quote bound to:
(engine version, rule catalog hash, receipt Merkle root, enclave
measurement).
3. Quote chains back to Intel / AMD root CA; customer verifier rejects
any binary that does not attest a known-good measurement.
4. Ship a TDX-runner lane in `action.yml` so GitHub-hosted TDX runners
(available on Azure DCsv3 / DCXsv3) can attest automatically.

**Crates**: `sgx-isa`, `sev`, `vtpm`, `webpki`, existing
`ed25519-dalek` + ML-DSA-65.

**Deliverables**:

* `crates/vault/src/tee\\\\\\\_attest.rs` — enclave launch + quote parsing
* `action.yml` TEE-runner lane
* `janitor-gov` quote verification endpoint

### P5-5 — Federated Differential Privacy Learning Across Tenants

**The gap**: Customer A discovers a novel supply-chain attack. Customer B
is vulnerable but neither knows. Cross-tenant learning today requires
data exfiltration, which violates the zero-upload guarantee.

**Build**:

1. Each customer's Janitor computes a local update (anomaly signature)
with (ε, δ)-differentially-private noise added at the node level.
2. Secure aggregation (Bonawitz et al. protocol) combines updates
without revealing individual contributions; aggregation keys bound
to existing PQC stack.
3. Global model of attack signatures distributed back as anonymized
rule updates.
4. Critical invariant: no customer source ever leaves the device, only
DP-noised structural aggregates.

**Crates**: `opendp` (DP primitives), `tokio`, existing PQC stack for
secure-aggregation keys.

**Deliverables**:

* `crates/gov/src/fedlearn.rs` — DP aggregation protocol
* `crates/forge/src/anon\\\\\\\_signature.rs` — signature emission + cache

\---

## Phase 6: Offensive AI Defense (24-48 Months)

The defensive war in 2028+ is fighting autonomous attackers at machine
speed. Stage 4 attackers split malicious intent across 100 bot accounts;
polymorphic malware rewrites itself across deployments; LLM-generated
code carries provider watermarks and stylometric signatures; ML model
weights ship with embedded backdoors. None of these are covered today.

### P6-1 — Multi-Account Swarm Attribution

**The gap**: Stage 4 attackers split malicious intent across 100 bot
accounts, each contributing benign-looking PRs, such that the COMPOSITE
is malicious but no single PR trips a rule. Today we score each author
individually.

**Build**:

1. Temporal correlation graph over all PRs landing in a window.
2. Cluster by:
(AST-style fingerprint, commit-timing cadence, network-egress pattern,
dependency-update co-occurrence).
3. Detect swarms via graph density thresholds + AGI-era behavioral
signatures (same vocabulary drift across accounts, synchronized
working hours, identical error-handling idioms).
4. Escalate finding to swarm level: "these 17 accounts collectively
introduced a capability-delegation chain across 5 files over 9 days."
5. Deterministic via fixed-seed clustering + rkyv-persisted baseline;
reproducible cross-session.

**Crates**: `petgraph`, `ndarray`, `smartcore` (DBSCAN, hierarchical
clustering), `rkyv`.

**Data structures**: `SwarmGraph { accounts, edges: Vec<TemporalEdge> }`;
`SwarmFinding { member\\\\\\\_ids, composite\\\\\\\_capability, confidence }`.

**Deliverables**:

* `crates/forge/src/swarm.rs` — temporal author clustering + composite
risk attribution
* `crates/gov/src/swarm\\\\\\\_events.rs` — multi-repo tenant aggregation

### P6-2 — Self-Modifying Malware Genome Tracker

**The gap**: Polymorphic malware (glassworm-class, advanced npm
supply-chain) rewrites itself across deployments. Static signature
matching fails. We need GENOMIC analysis: track structural invariants
across mutation generations so that behavior-preserving cosmetic
rewrites still trigger detection.

**Build**:

1. Extract MinHash + SimHash fingerprints of AST subtree structure per
commit.
2. Track "genome drift" across package versions: which invariants
(call-sequence topology, side-effect ordering, network-fingerprint)
persist through cosmetic rewrites?
3. Detect: invariant call sequences preserved through source-level
obfuscation; flag packages whose behavioral genome matches a
known-malicious signature within tolerance.
4. Integrates with the existing slopsquat embedded corpus — genome
matching replaces the current name-based lookup for the top risk
packages.

**Crates**: `minhash`, `simhash`, `petgraph`, `fst` (genome-set
membership), `rkyv` (genome persistence).

**Deliverables**:

* `crates/forge/src/malware\\\\\\\_genome.rs` — cross-version genome extractor
* mutation-invariant matcher
* Slopsquat corpus promoted from name-based to genome-based matching

### P6-3 — Neural Model Weight Backdoor Scanner

**The gap**: ML models shipping as `.safetensors` / `.gguf` / `.pt` can
carry BadNet backdoors — trigger inputs that activate hidden behavior
(image classifier that mislabels anything with a specific sticker;
language model that emits secrets when a trigger token appears). Zero
current coverage. The 2028+ software supply chain is 50% model weights;
the SAST market ignores them entirely.

**Build**:

1. Parse `.safetensors` / GGUF / PyTorch state-dict headers (all
structured formats).
2. Extract weight tensors; run:
a. Neuron-activation anomaly detection (statistical outliers vs.
baseline architectures of the same family).
b. Trigger-pattern reverse-engineering (Neural Cleanse algorithm
over bounded search space).
c. Embedding-drift analysis against a model-provenance registry
(HuggingFace attested checkpoints).
3. Flag weights that deviate from the declared architecture's expected
statistical profile.
4. Deterministic via fixed-seed optimization + bounded search;
reproducible across runs.

**Crates**: `safetensors`, `ggml-rs`, `candle-core`, `ndarray`, `linfa`
(anomaly detection).

**Deliverables**:

* `crates/anatomist/src/model\\\\\\\_ingest.rs` — weight-file parser
* `crates/forge/src/model\\\\\\\_backdoor.rs` — activation-anomaly +
trigger-reverse detector

### P6-5 — LLM Provenance \& Prompt-Injection Sink Detection

**The gap**: "Every commit must be signed" assumes human authorship.
Reality: 40%+ of code in 2026 is LLM-generated. No engine today
distinguishes human vs. LLM origin, or detects the specific
LLM-provider signatures (Claude, GPT, Gemini have distinguishable
statistical fingerprints).

**Build**:

1. Stylometric classifier over AST features + token-distribution
features; local bounded model via `candle`.
2. Correlate with provider-specific generation signatures (public
watermarking schemes from major LLM providers).
3. Emit `llm\\\\\\\_provenance` flag per commit: `{human, claude, gpt, gemini, unknown}` + confidence.
4. Separately: detect prompt-injection SINKS — places where untrusted
data flows to `Prompt::from(untrusted\\\\\\\_string)`,
`system\\\\\\\_message(user\\\\\\\_input)`, or any
`Agent.invoke(possibly\\\\\\\_injected\\\\\\\_input)`.
5. Pair with P6-4 agentic graph so the provenance flag informs the
escalation analysis.

**Crates**: `candle-core` (bounded, offline), `ndarray`, existing taint
engine.

**Deliverables**:

* `crates/forge/src/llm\\\\\\\_provenance.rs` — stylometric LLM-origin
classifier
* `crates/forge/src/prompt\\\\\\\_injection.rs` — prompt-injection sink
registry + taint detection lane

\---

## Phase 7: Temporal Sovereignty (Ongoing)

Time-domain capabilities: looking backward across history, sideways
across repos, and forward across patch horizons.

### P7-1 — Chronovisor: Retrospective Vulnerability Archaeology

**The gap**: A detector shipped today cannot find historical bugs
without a re-scan of every historical commit. Competitors re-analyze
on-demand; we can re-analyze THE PAST with new rules, turning every
future rule into a retroactive advisory for every customer's full
history.

**Build**:

1. `git log --all --reverse` walk; for each commit, mount the working
tree via `git2` and run the full detector suite.
2. Persist findings against `(commit\\\\\\\_sha, rule\\\\\\\_id)` tuple in an
`ArchiveLedger` (rkyv + BLAKE3-sealed).
3. On new rule rollout, incrementally analyze the historical tree for
first-occurrence of the vuln class.
4. Emit `Chronovisor::first\\\\\\\_introduction(rule) -> commit\\\\\\\_sha` per rule
per repo — gives the defender a precise "when did we become
vulnerable" answer.
5. Bounded by the Physarum backpressure gate; parallelism via rayon
* `detect\\\\\\\_optimal\\\\\\\_concurrency()`.

**Crates**: `git2` (existing), `rkyv`, `rayon`, `memmap2`.

**Deliverables**:

* `crates/anatomist/src/chronovisor.rs` — history walker + rule-replay
engine
* `crates/cli/src/main.rs::cmd\\\\\\\_chronovisor` — operator entrypoint
* Incremental cache so re-running with an added rule is O(new rule ×
commits) not O(rules × commits)

### P7-2 — Real-Time Patch Correctness Proof

**The gap**: A "fix" for a vuln may fix the symptom but leave the
invariant broken, or introduce a new unrelated bug. Today we re-scan
and confirm the specific finding is gone. That does not prove the patch
didn't introduce a new bug elsewhere.

**Build**:

1. AST diff between pre-patch and post-patch function.
2. Bisimulation equivalence checker over the non-fix regions.
3. Kani harness verifies the new function satisfies the safety property
that the old function violated.
4. Emit `PatchProof::{ Equivalent\\\\\\\_Except\\\\\\\_For\\\\\\\_Fix | Introduces\\\\\\\_New\\\\\\\_Behavior | Unsatisfiable }`.
5. Couples with P4-1 verifier and the existing Z3 path-feasibility lane.

**Crates**: `rsmt2` / `z3` (existing), `kani` (from P4-1), existing
taint spine.

**Deliverables**:

* `crates/forge/src/patch\\\\\\\_proof.rs` — AST diff + bisimulation engine +
Kani bridge

### P7-3 — Binary Diff Exploit Prediction (1-Day Synthesis)

**The gap**: When a vendor ships a patched binary, attackers BinDiff
patched vs. unpatched, locate the fix, and synthesize a 1-day exploit
before customers patch. We can do this FOR the defender — predict which
CVE classes are exploitable in a customer's unpatched artifacts.

**Build**:

1. For a customer's binary artifact (APK, `.exe`, `.dylib`, OCI image
layer), maintain a cached disassembly + CFG.
2. When a CVE's patched binary is published, compute binary diff via a
BinDiff-style algorithm (MD-index / Kruskal-over-bipartite-matching).
3. Map the patched region to a vuln class signature.
4. Query whether the customer's binary contains the pre-patch pattern.
5. Emit a patch-urgency score based on exploit-proximity (how close the
attacker's public diff is to a weaponized 1-day).

**Crates**: `goblin`, `iced-x86`, `capstone`, `fst` (signature
registry), `rkyv`.

**Deliverables**:

* `crates/forge/src/bindiff\\\\\\\_predict.rs` — binary-diff engine
* `crates/anatomist/src/cve\\\\\\\_bindiff.rs` — CVE-to-signature mapper
* Integration with the existing KEV feed so bindiff prediction is
automatically triggered on KEV updates

### P7-4 — Continuous Regulatory Compliance Oracle

**The gap**: FedRAMP, PCI-DSS, SOC2, HIPAA, EO 14028, NIS2, DORA, CRA —
each has different evidence requirements. Customers cobble together
reports manually, and compliance officers spend months reconciling SAST
output to control frameworks.

**Build**:

1. Compliance-schema catalog mapping each framework's controls to
specific Janitor rule sets.
2. On-scan emit `ComplianceReceipt { framework, control\\\\\\\_id, status, evidence\\\\\\\_capsule }`.
3. Aggregate across scans into an auditor-ready dossier.
4. Cryptographically sealed via existing PQC + SHA-384 audit-ledger
stack.
5. Framework-specific diff mode: "what changed since last audit that
affects PCI-DSS control X.Y?"

**Crates**: existing PQC + SARIF + CEF stack, `rkyv`, `roxmltree` for
framework control-catalog ingestion.

**Deliverables**:

* `crates/gov/src/compliance.rs` — framework-to-rule mapping registry
* `crates/cli/src/main.rs::cmd\\\\\\\_compliance\\\\\\\_report` — auditor-ready
evidence emitter
* Pre-populated mappings for the top seven frameworks

\---

## Phase 8: Domain Specialization Lanes (Parallel Fast-Track)

Verticals where depth unlocks markets no horizontal SAST can touch.
Each lane is independently shippable and independently monetizable.

### P8-1 — OT / ICS / SCADA Analysis Pack

**The gap**: Industrial control systems (Stuxnet-class targets) run
Modbus, DNP3, IEC 61850, OPC-UA. Zero SAST vendor covers ladder-logic,
function-block diagram, or structured-text semantics meaningfully.
Market: critical-infrastructure operators, insurers, nation-state
defenders.

**Build**:

1. IEC 61131-3 Structured Text grammar (author a tree-sitter grammar
in-house — no upstream grammar exists).
2. Modbus / DNP3 message-parsing sink registry.
3. Ladder-logic flow extractor from PLC export XML formats (Siemens,
Allen-Bradley, Schneider).
4. Detect: default-password PLC credentials, unauthenticated remote
function codes, race conditions in safety-interlock chains,
hardcoded engineering unit overrides.

**Crates**: custom tree-sitter grammar (in-house), `roxmltree`,
`petgraph`.

**Deliverables**:

* `crates/anatomist/src/ics.rs` — IEC 61131-3 parser
* `crates/forge/src/ics\\\\\\\_rules.rs` — ICS-specific sink detectors

### P8-2 — Automotive / Aerospace ECU Lane

**The gap**: AUTOSAR C, MISRA C compliance, DO-178C aerospace
certification, CAN bus message safety. Safety-critical code has distinct
failure semantics and a captive market with regulatory budgets
(automotive OEMs, tier-1 suppliers, defense primes).

**Build**:

1. MISRA C rule enforcement via existing AhoCorasick layer.
2. CAN DBC file parser for message-taint flow into ECU firmware.
3. DO-178C traceability matrix: requirement → code → test coverage
with sealed receipt.
4. AUTOSAR classic / adaptive service-interface consistency checks.

**Crates**: `dbc-parser`, existing C tree-sitter, `rkyv`.

**Deliverables**:

* `crates/forge/src/automotive.rs` — CAN message taint + MISRA
enforcement + DO-178C traceability

### P8-3 — Medical Device Pack (IEC 62304 / HL7 / FHIR)

**The gap**: Medical device software needs IEC 62304 traceability + FDA
510(k) compliance. FHIR APIs leak PHI if unbounded. Captive market:
medical device OEMs, hospital IT, health-insurance payors.

**Build**:

1. HL7 / FHIR sink registry for PHI-carrying fields (name, DOB,
diagnosis, genomic identifiers).
2. IEC 62304 level-of-concern classifier (Class A / B / C).
3. Audit-log presence verifier (FDA §820.180 record retention).

**Crates**: existing tree-sitter + sink taxonomy.

**Deliverables**:

* `crates/forge/src/medical.rs` — PHI sink registry + 62304 evidence
emitter

### P8-4 — Kernel Exploit Primitive Catalog

**The gap**: Linux kernel exploitation requires heap-shaping gadgets,
`modprobe\\\\\\\_path` / `core\\\\\\\_pattern` escalation paths, SLAB allocator
abuse. Generic C SAST misses these entirely. Market: kernel developers,
container-runtime vendors, EDR teams.

**Build**:

1. Curated registry of kernel-specific sinks:
`copy\\\\\\\_from\\\\\\\_user` without size check → OOB write; `kmalloc(size, GFP\\\\\\\_KERNEL)` with attacker-controlled size → heap spray;
`call\\\\\\\_usermodehelper` with attacker-controlled path.
2. Detect common primitives (UAF, OOB, type confusion) via specialized
taint lanes with kernel-aware lifetime model.
3. Emit exploit-class classification (`LPE | ContainerEscape | RCE`).

**Crates**: existing C tree-sitter, existing taint engine, `rkyv`.

**Deliverables**:

* `crates/forge/src/kernel.rs` — kernel-specific sink taxonomy +
primitive detector

### P8-5 — Browser Extension \& WebAssembly Static Pack

**The gap**: MV3 Chrome extensions, Firefox add-ons, Safari extensions
have unique permission models. Wasm modules in production compose
differently from native code. We already execute Wasm (BYOP sandbox);
we do not yet statically analyze submitted Wasm modules.

**Build**:

1. `manifest.json` V3 permission parser.
2. Content-script ↔ background-worker message-flow taint lane.
3. Wasm import analysis: extend existing wasmtime integration to
analyze Wasm rather than only execute it (imports, exports, memory
growth signatures, compute budget estimates).

**Crates**: existing wasmtime, `serde\\\\\\\_json`, existing JS/TS
tree-sitter.

**Deliverables**:

* `crates/forge/src/browser\\\\\\\_ext.rs` — MV3 permission model
* `crates/forge/src/wasm\\\\\\\_static.rs` — Wasm module static analyzer

\---

## Phase 9: Bio-Computational \& Frontier Extensions

Speculative but architecturally concrete. These are the dark-horse
capabilities — one of them, fully executed, reshapes the market.

### P9-1 — Physarum Immune System Evolution

**The gap**: The current Physarum subsystem handles concurrency
backpressure. A true immune system has memory, affinity maturation, and
self/non-self recognition — biological concepts with direct analogues
in long-lived adaptive defense.

**Build**:

1. `MemoryCell` records — persistent vuln-pattern signatures that matured
from repeated exposure across customer deployments.
2. `AffinityMaturation` engine — refines rules over time based on
confirmed true-positives; mutation-select cycle bounded by Crucible
fixtures to prevent rule drift.
3. `Self / Non-Self` classifier — learns each tenant's baseline (commit
cadence, test patterns, approved dependencies, typical AST idioms)
and flags non-self anomalies even when no pattern rule matches.
4. Deterministic via fixed-seed mutation RNG; reproducible across
sessions.

**Crates**: existing Physarum + `ena` (union-find for matured-rule
families), `rkyv`.

**Deliverables**:

* `crates/common/src/immunity.rs` — memory cells + affinity maturation
state machine

### P9-2 — Stateful Hash-Based Post-Quantum Provenance (LMS / XMSS)

**The gap**: ML-DSA + SLH-DSA are our current PQC stack. For
infinite-retention audit logs, stateful hash-based signatures (XMSS,
LMS, RFC 8554 / 8708) offer smaller signatures with forward-secure
properties, at the cost of state management. Enterprise customers with
30-year retention requirements (regulatory archives, legal discovery)
need this or they accept a forward-security gap.

**Build**:

1. Integrate `xmss-rs` or hand-rolled LMS (RFC 8554).
2. Tree-based signing for audit-ledger entries.
3. Collision-resistant without number-theoretic assumptions.
4. Stateful key management (single-use one-way signing tree) with
failure-safe state persistence.
5. Hedged against a future break of ML-DSA / SLH-DSA — hash-based
signatures rely only on the one-wayness of the hash, which is the
most conservative cryptographic assumption we can make.

**Crates**: `xmss-rs`, existing BLAKE3 / SHA-384 stack.

**Deliverables**:

* `crates/vault/src/stateful\\\\\\\_pqc.rs` — LMS / XMSS signer for audit
ledger; strict state persistence guard

### P9-3 — Decentralized Rule Mesh (Libp2p + Content-Addressed)

**The gap**: Wasm rule marketplace (P3-4) assumes central distribution
(warg-protocol). True sovereign ecosystems ship rules peer-to-peer with
content-addressed integrity. Defense against vendor lock-in: the rule
mesh is independent of any single operator staying alive.

**Build**:

1. libp2p-based rule mesh; each rule has a content address (BLAKE3
hash) and provenance chain (signed by author chain-of-custody).
2. Consumers subscribe to rule feeds from multiple authors; default-deny
with allowlist of signer keys.
3. Gossipsub propagation with bounded fan-out to prevent spam-rule
floods.
4. Rule-revocation events as first-class gossip messages.

**Crates**: `libp2p`, existing PQC + BLAKE3.

**Deliverables**:

* `crates/gov/src/mesh.rs` — P2P rule distribution
* Content-addressed signed rule-bundle format

### P9-4 — Semantic Dependency Graph Over Cross-Language FFI Boundaries

**The gap**: A Rust crate may call a C FFI shim which wraps a Python
module which loads a Wasm blob. Taint crosses four language boundaries.
We currently stop at Rust FFI. The decade's hardest bugs live at FFI
interfaces — memory-model mismatches, ownership surprises,
type-confusion across ABIs.

**Build**:

1. Unified `InterLanguageCallGraph` where each node is `(language, symbol, ABI\\\\\\\_spec)` and edges are `FFI\\\\\\\_call | PyO3 | JNI | wasmtime\\\\\\\_export | cffi | ctypes | NAPI | neon`.
2. Extend the taint catalog to carry ABI metadata across boundaries
(size, endianness, ownership transfer, nullability).
3. Detect: ownership-model mismatch (Rust `\\\\\\\&mut T` passed across FFI
to a caller that retains the pointer), type-confusion across JNI,
Wasm linear-memory aliasing with host memory.

**Crates**: existing polyglot, `petgraph`, `rkyv`.

**Deliverables**:

* `crates/forge/src/ffi\\\\\\\_taint.rs` — cross-language taint propagation
with explicit ABI normalization

### P9-5 — Adversarial-Robust Rule Evolution (Genetic Programming)

**The gap**: Attackers adapt to rules faster than we write them. We need
rules that adapt back — deterministically, sealed, with bounded mutation
so we never drift into undecidable rule families.

**Build**:

1. Represent a detection rule as a typed AST (tree-sitter query +
predicate chain).
2. Population of rules evolves via mutation (predicate addition,
pattern generalization) + crossover.
3. Fitness = (true-positive rate × generality) / false-positive rate;
measured against the Crucible fixture corpus.
4. Mutation seeds drawn from confirmed attack corpora (Crucible
fixtures + KEV catalog).
5. Bounded generations, deterministic RNG (`rand\\\\\\\_chacha` with
fixed seed), sealed via provenance.
6. Every evolved rule ships with the full lineage chain back to its
seed rule — we can audit exactly how a rule matured.

**Crates**: existing tree-sitter, `rand\\\\\\\_chacha` (deterministic), `rkyv`.

**Deliverables**:

* `crates/forge/src/rule\\\\\\\_evolution.rs` — typed-AST rule encoder +
genetic evolution engine with deterministic RNG seeding
* Lineage-tracked rule emission with Crucible regression gating on
every generation

\---

## Architectural Invariants (Non-Negotiable Across All Phases)

1. **Determinism is sacred.** Every analysis path must produce identical
results given identical inputs. No floating-point non-determinism, no
thread-ordering sensitivity, no wall-clock-dependent decisions. ML
ranks; rules decide.
2. **Zero-upload guarantee.** Source code never leaves the device. No
cloud API calls for analysis. The Janitor is air-gap-capable today and
must remain so through every phase, including P5 (ZK / FHE / iO / TEE
channels emit only attestations, never source).
3. **Bounded resource consumption.** Every new analysis phase must
respect the Physarum backpressure system and the 8 GB Law. Symbolic
execution gets a 10,000-state budget. IFDS gets a per-function summary
cache cap. Genetic rule evolution gets a bounded generation count.
Unbounded analysis is denial-of-service against the operator.
4. **Provenance continuity.** Every finding, at every phase, is sealed
into a `DecisionCapsule` with analysis parameters, engine version,
taint catalog hash, rule set hash, and Wasm policy receipts. A finding
without provenance is not a finding.
5. **Fail-open on analysis, fail-closed on trust.** A parse failure,
solver timeout, or budget exhaustion produces a degraded result (fewer
findings), never a crash or a false "all clear." The base
pattern-matching tier is always available as a floor. Conversely, any
trust-anchor failure (PQC signature invalid, enclave quote rejected,
rule lineage broken) is a hard-stop — trust never degrades gracefully.
6. **Structural gate mandate.** Every new phase that detects a
vulnerability must also contribute a Crucible fixture (true-positive
* true-negative) so the detection is permanently regression-guarded.
No phase ships without its Crucible entry.

\---

## Competitive Kill Chain

|Competitor|Their Moat|Our Counter|
|-|-|-|
|Semgrep|Rule ecosystem, developer adoption|IFDS + Z3 proves exploitability; their pattern-match rules cannot. iO-sealed Wasm marketplace (P5-3) matches their rule count with cryptographic provenance they lack.|
|CodeQL|Datalog queries, GitHub integration|Deterministic + zero-upload + air-gap-capable + ZK attestation (P5-1). CodeQL requires cloud. SLED / FedRAMP / DoD / classified customers cannot use it.|
|Snyk|Developer workflow integration|Artifact ingestion (OCI / wheel / IPA / ASAR / JAR / APK) + neural model weight scanning (P6-3) cover shipped artifacts they cannot analyze.|
|SonarQube|Enterprise installed base|Symbolic execution + full-spectrum AEG (P3-1 Phases B-E) produces exploit witnesses across HTTP / serialized / protocol / smart-contract / parser domains; SonarQube produces informational findings.|
|Checkmarx|Enterprise SAST market share|Cross-service campaign planner (P3-2) + gadget atlas (P2-5) + RCAL (P4-4) produces root-cause chains; Checkmarx produces per-file findings.|
|Trail of Bits (Slither)|Solidity dominance|Multi-chain support (Solidity + Vyper + Move + Cairo) with formal verification (P4-1). Slither is Solidity-only and has no formal-verification export.|
|Endor Labs / Chainguard|Supply-chain SBOM dominance|Genome-based malware tracking (P6-2) + FFI taint across language boundaries (P9-4) + ZK attestation — their SBOM is descriptive, ours is provable.|
|ML supply-chain vendors (Protect AI, HiddenLayer)|Neural model scanning first-mover|Native model weight scanner (P6-3) integrated with source-level analysis + agentic execution graph (P6-4) — unified defensive plane instead of a siloed model scanner.|
|Manual bounty hunters|Domain expertise, creativity|Full-spectrum AEG generates PoC payloads faster than manual reverse engineering. Campaign planner finds multi-hop chains humans miss. Hunters use The Janitor as a force multiplier, not a competitor.|
|Nation-state red teams|Unlimited resources, target intelligence|Temporal swarm attribution (P6-1) + Labyrinth (P3-6) + TEE-sealed envelopes (P5-4) — the only defensive stack whose cost model *grows* faster than the attacker's as they scale.|

\---

## Non-Negotiable Thesis

The Janitor is already the strongest deterministic structural interceptor
in existence, with interprocedural taint and Z3 path-feasibility shipped.
The decade-defining moat is **semantic correctness** and **adversarial
AI defense**: formal-method-backed safety proofs, intent-vs-implementation
divergence detection, cryptographic protocol verification, full-spectrum
exploit witness synthesis, neural model weight scanning, agentic
execution graphing, and temporal-and-spatial offensive-AI attribution.

Enterprise SAST dominance comes from proving invariants — not findings.
$100k+ bug-bounty dominance comes from producing full-spectrum exploit
witnesses (HTTP + serialized + protocol + on-chain + parser + agentic-tool-chain)
faster than any human hunter can manually reverse the target. $1M+
enterprise moats come from attestations — ZK proofs, TEE-sealed
envelopes, iO-obfuscated signed rules, federated DP learning — that
cloud-only competitors cannot even architecturally provide.

The engine that proves invariants wins the decade.
The engine that matches patterns wins the quarter.
The engine that defends against autonomous AI attackers at machine speed
wins the century.

---

## Phase 10: The Sovereign Endpoint (10+ Years)

### P10: Zero-Knowledge Sovereign Silicon (ZK-AST)

**Proposal:** Execute AST extraction inside Intel TDX / AMD SEV-SNP enclaves.
Emit zk-SNARKs proving the absence of critical bugs without disclosing
proprietary source code.

### P11: Fully Homomorphic Taint Tracking (FHE)

**Proposal:** Evaluate IFDS policies over encrypted AST structures for
classified air-gapped environments.

### P12: The Non-Computable Deception Plane

**Proposal:** Embed undecidable AST structures (Godel traps) into the codebase
to bankrupt adversarial AI context windows.
