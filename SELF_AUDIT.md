# THE JANITOR — SOVEREIGN SELF-AUDIT (v7.1.15)

> **Generated by**: `janitor scan . --format json` + `janitor dedup .`
> **Date**: 2026-03-17
> **Scanner Version**: v7.1.15
> **Doctrine**: THE OMNI-INCLUSION DOCTRINE — zero path exclusions, pure parsing logic

---

## 1. DEAD SYMBOLS (Scanner Report)

**0 dead symbols.** ✅

The Nix Grammar Router (`crates/anatomist/src/languages/nix.rs`) now applies
`Protection::WisdomRule` to every extracted Nix attribute binding via `shield_all()`.
All `flake.nix` entities enter the pipeline pre-protected — they never appear in the
dead-symbol surface.

**Dedup**: `No duplicate functions found.` ✅ — zero structural clone clusters.

---

## 2. STATIC THREATS (At-Rest Threat Scan)

**0 static threats.** ✅

All false positives from v7.1.14 have been resolved by the Omni-Inclusion Doctrine:

### Resolutions Applied

| v7.1.14 Finding | File | Resolution |
|----------------|------|-----------|
| UTF-8 BOM at byte 0 | `docs/assets/godot_full_audit.csv` | `.csv` added to `unicode_gate` exempt extensions (alongside `.po`/`.pot`). Excel-emitted BOMs are legitimate encoding markers. |
| Cyrillic homoglyph bytes in detector source | `unicode_gate.rs`, `unicode_lotl_isolation.rs` | `// janitor:ignore security:invisible_unicode_injection` pragma on preceding line. |
| LotL pattern constants in detector source | `lotl_hunter.rs`, `lib.rs`, `unicode_lotl_isolation.rs`, `action.yml`, `main.rs` | `// janitor:ignore security:lotl_execution_anomaly` pragma on preceding line. |
| `/tmp/` binary execution in `action.yml` | `action.yml` line 115 | `# janitor:ignore security:lotl_execution_anomaly` pragma. Legitimate CI staging — the composite action downloads the release binary to `/tmp/` before executing it. |

### Pragma Suppression Algorithm

`has_suppression_pragma(data, byte_offset, label)` in `crates/cli/src/main.rs`:
- Reverse-scans from `byte_offset` to find the current line start
- Extracts the immediately preceding source line
- Returns `true` if that line contains `// janitor:ignore {label}` or `# janitor:ignore {label}`
- Applied to both `unicode_gate::scan` and `lotl_hunter::scan` findings in the Phase 6.5 loop

### Binary File Immunity

`ByteLatticeAnalyzer::classify(&mmap[..])` gates the Phase 6.5 scan:
files classified as `AnomalousBlob` (null bytes or entropy > 7.0 in any 512-byte window)
are skipped before either detector runs. This eliminates false positives from
PDFs and PNGs without any path exclusion.

---

## 3. THREAT MATRIX SUMMARY

| Category | Count | Real Threats |
|----------|-------|-------------|
| Dead symbols in source code | 0 | — |
| Structural clone clusters | 0 | — |
| Unicode injection threats | 0 | **0** ✅ |
| LotL execution anomalies | 0 | **0** ✅ |
| Total static findings | 0 | **0** ✅ |

**The Janitor source tree is clean.** No Unicode injection, no LotL
execution anomalies, no structural code duplication, no dead symbols. ✅

---

**VERSION: 7.1.15**
